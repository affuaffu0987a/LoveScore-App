 ```
# Love Score-Board

This is a simple React application that generates a random love score between 0 and 100. The user can input their name and their partner's name, and the app will generate a score based on their names. The app also has a button that allows the user to refresh the score.



## Code Explanation

The code for this app is relatively simple. The main component is the `App` component. This component renders the main layout of the app, including the header, the input fields, and the button. The `App` component also handles the state of the app, such as the user's names and the love score.

The `Scoreboard` component is responsible for generating the love score. This component takes the user's names as props and uses a random number generator to generate a score between 0 and 100. The `Scoreboard` component also renders the love score to the user.

The `index.css` file contains the CSS styles for the app. The CSS styles are used to style the layout of the app, such as the colors, fonts, and sizes of the elements.

## Conclusion

This is a simple React application that demonstrates how to generate a random love score. The app is easy to use and can be customized to fit your needs.

